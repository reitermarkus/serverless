import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    flexDirection: 'column',
  },
  headLine: {
    fontSize: 32,
    textAlign: 'center',
    padding: 20
  },
  subHeadline: {
    fontSize: 20,
    paddingBottom: 15,
    paddingLeft: 15
  }
})

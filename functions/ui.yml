version: 1.0
provider:
  name: openfaas
  gateway: http://127.0.0.1:8080
functions:
  ui:
    lang: static
    handler: ./ui
    image: ui:latest
    environment:
      write_debug: 'true'
      gateway_url: http://gateway:8080
